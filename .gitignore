__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.DS_Store
# outputs (anchored to the repo root: an unanchored `report/` would also
# match the src/motifmultiverse/report/ module)
/registry/
/evidence/
/lexicons/
/validation/
/inference/
/report/
*.h5
*.npz
*.parquet
# local scratch working artifacts; never tracked
/.superpowers/

# Generated per run by `python -m motifmultiverse.status`, and per-run facts do
# not belong in version control: the backend rows and test counts inside it are
# true of the machine that produced it and of nothing else. CI regenerates and
# uploads it on every run.
implementation_status.json
